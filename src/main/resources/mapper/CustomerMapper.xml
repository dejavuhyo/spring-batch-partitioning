<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.batch.mapper.CustomerMapper">

    <select id="findByTimeRange" resultType="com.example.batch.entity.Customer">
        SELECT id, name, processed, updated_at
        FROM customer
        WHERE processed = false
          AND updated_at &gt;= CAST(#{minTime} AS TIMESTAMP)
          AND updated_at &lt; CAST(#{maxTime} AS TIMESTAMP)
        ORDER BY updated_at ASC, id ASC
            LIMIT #{_pagesize}
        OFFSET #{_skiprows}
    </select>

    <update id="updateStatus">
        UPDATE customer
        SET processed = true
        WHERE id = #{id}
    </update>

</mapper>
